fn main() {
    // TODO: Change the line below to fix the compiler error.
    let x:i32 = 10; // 这里需要补充类型，否则无法==10；此外还需要赋初值，否则无法引用x

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
